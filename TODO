Config API:
	Basic API Functions:
		☐ get_current_config
			Returns the user selected config
		☐ get_configurations
			Returns all configurations created by the user
		Limited by whether accessed by internal modules or plugins
	Internal Modules:
	Plugins:
		☐ Expose through Plugin Manager(PluginManager.get_config is a start but needs to be implemented and expanded)

Main.py:
	☐ Calls the Auto-Updater
	☐ Instantiates the Core and starts the loop
	☐ Catches any errors, and exports them appropropriately

Tests:
	Beryl:
		Event Manager:
			☐ Test registering listeners
			☐ Test triggering listeners
	Plugin Helper:
		PluginBase:
			✔ Call the proper register listeners @done(19-08-15 09:08)
			☐ Get cross plugin data
			☐ Set cross plugin data
